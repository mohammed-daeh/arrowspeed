import 'package:flutter/material.dart';

class ReferAndEamScreen extends StatelessWidget {
  const ReferAndEamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ReferAndEamScreen'),));
  }
}